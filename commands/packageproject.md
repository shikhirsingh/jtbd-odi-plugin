---
description: Alias for /exportdeliverables — bundle the six Table 30.1 artifacts into a shippable deliverables/ folder.
argument-hint: [--project-dir .] [--zip]
---

Invoke the `exportdeliverables` skill. This is an alias.

See `/exportdeliverables` for the full plain-English preamble — what the six artifacts are, what the coverage report proves, why the skill refuses to ship when any artifact is missing.
