---
description: Alias for /computescores — produces the opportunity scores table + the Opportunity Landscape chart. Named after the chart it produces.
argument-hint: <path to cleaned survey CSV>
---

Invoke the `computescores` skill. This is an alias.

Same plain-English help applies — see `/computescores` for the full preamble.
